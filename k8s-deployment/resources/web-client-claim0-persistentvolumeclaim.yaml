apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: web-client-claim0
  name: web-client-claim0
  namespace: nonprod
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
