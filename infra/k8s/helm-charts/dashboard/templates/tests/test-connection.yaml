apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reconmap-dashboard.fullname" . }}-test-connection"
  labels:
    {{- include "reconmap-dashboard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reconmap-dashboard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
