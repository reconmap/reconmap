apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: dashboard
  name: dashboard
  namespace: nonprod
spec:
  ports:
    - name: "5500"
      port: 5500
      targetPort: 5500
  selector:
    app.kubernetes.io/name: dashboard
